<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>KYC Verification - BanMarket</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Favicon -->
  <link href="/images/ban-invest.png" rel="icon">
  
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body {
      background-image: linear-gradient(to right, rgba(26, 0, 142, 0.658), rgba(129, 0, 0, 0.5)),url(../images/invest.jpg);
    background-size: cover; /* Ensures the background covers the entire page */
    height: auto; /* Ensures the body takes the full height of the viewport */
    background-repeat: no-repeat;
  }
    input[type="file"]::file-selector-button {
      background-color: #2563eb;
      color: white;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 0.375rem;
      cursor: pointer;
    }

    input[type="file"]::file-selector-button:hover {
      background-color: #1e40af;
    }
  </style>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center p-6">

  <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-lg">
    <h2 class="text-2xl font-bold text-center mb-4">KYC Verification</h2>

    <p class="text-gray-700 text-sm mb-6 text-center">
      To ensure compliance with financial regulations and protect your account, KYC (Know Your Customer) verification is required.
      <br><br>
      <strong>You can deposit funds</strong>, but <span class="text-red-600 font-semibold">cannot invest or withdraw</span> until your identity is verified.
    </p>

    <% if (message) { %>
      <div class="bg-yellow-100 text-yellow-800 text-sm font-medium p-3 rounded mb-4">
        <%= message %>
      </div>
    <% } %>

    <form action="/upload-kyc" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
      <label class="text-sm font-medium">Upload Government Issued ID (PDF or Image):</label>
      <input
        type="file"
        name="kyc_document"
        accept=".pdf, .jpg, .jpeg, .png"
        required
        class="border border-gray-300 rounded-lg p-2 bg-gray-50"
      />

      <button type="submit" class="bg-blue-600 hover:bg-blue-800 text-white font-semibold py-2 rounded-lg">
        Submit KYC
      </button>
    </form>
  </div>

</body>
</html>
