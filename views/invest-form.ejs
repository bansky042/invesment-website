<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= planName.charAt(0).toUpperCase() + planName.slice(1) %> Investment</title>
  <style>
    body {
      background-color: #eef2f5;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .form-box {
      background: #fff;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 400px;
    }

    h2 {
      margin-bottom: 20px;
      color: #333;
    }

    input, button {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 16px;
    }

    button {
      background-color: #4caf50;
      color: white;
      border: none;
    }

    button:hover {
      background-color: #45a045;
    }

    .message {
      color: red;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="form-box">
    <h2>Invest in <%= planName.charAt(0).toUpperCase() + planName.slice(1) %> Plan</h2>
<p>Your available deposit balance: <strong>$<%= depositBalance %></strong></p>
 <p>Min: $<%= planDetails.min %> | Max: $<%= planDetails.max === Infinity ? '∞' : planDetails.max %></p>
    <p>Profit: <%= planDetails.percent %>%</p>
    <form action="/invest/<%= planName %>" method="POST" onsubmit="return validateAmount()">
      <input type="number" name="amount" placeholder="Enter amount" required />
      <button type="submit">Start Investment</button>
    </form>
    <% if (message) { %>
      <div id="statusMessage" class="message text-green-600">
        <%= message %>
      </div>
      <script>
        setTimeout(() => {
          window.location.href = '/invest';
        }, 4000);
      </script>
    <% } %>

  </div>
</body>
</html>
